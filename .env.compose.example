# Description: Example environment for Docker Compose.
# Usage: Copy this file to .env.compose and run:
#   docker compose --env-file .env.compose up -d

NODE_ENV=production
FASTIFY_ADDRESS=0.0.0.0
FASTIFY_PORT=3000
MONGO_URI=mongodb://mongodb:27017/usthing
AUTH_SKIP=false
